** 3314. Construct the Minimum Bitwise Array I **

# Approach 1:
class Solution:
    def minBitwiseArray(self, nums: List[int]) -> List[int]:
        ans = []

        for p in nums:
            found = -1

            for x in range(p + 1):
                if (x | (x + 1)) == p:
                    found = x
                    break

            ans.append(found)

        return ans

# Approach 2:
class Solution:
    def minBitwiseArray(self, nums: List[int]) -> List[int]:
        ans = []
        for i in nums:
            for j in range(i):
                if j | (j + 1) == i:
                    ans.append(j)
                    break
            else:
                ans.append(-1)
        return ans
