** 2110. Number of Smooth Descent Periods of a Stock **

# Approach 1:
class Solution:
    def getDescentPeriods(self, prices: List[int]) -> int:
        n = len(prices)
        res = 1
        curr = 1
        for i in range(1, n):
            if prices[i - 1] - prices[i] == 1:
                curr += 1
            else:
                curr = 1
            res += curr
        return res

# Approach 2:
class Solution:
    def getDescentPeriods(self, prices: List[int]) -> int:
        length = len(prices)
        periods = length
        i = 0
        while i <= length - 2:
            streak = 0
            while  i <= length - 2 and prices[i + 1] == prices[i] - 1:
                streak += 1
                i += 1
                periods += streak 
            i += 1
        return periods
